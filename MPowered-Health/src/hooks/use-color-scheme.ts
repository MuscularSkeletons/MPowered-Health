// This hook reads the device's current light or dark color setting.
// Native platforms already provide this behavior, so the project reuses it directly.
export { useColorScheme } from 'react-native';
